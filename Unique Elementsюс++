#include <iostream>
using namespace std;

int main() {
    int a[6]={1,2,2,3,4,4};

    for(int i=0;i<6;i++) {
        bool unique = true;
        for(int j=0;j<6;j++) {
            if(i!=j && a[i]==a[j]) unique=false;
        }
        if(unique) cout << a[i] << " ";
    }
}
